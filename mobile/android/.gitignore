# Machine-specific — generated by Android Studio / Gradle, points at the local SDK install
local.properties

# Build output and Gradle caches
build/
.gradle/
*.iml

# Native build intermediates (CMake/NDK)
.cxx/
.externalNativeBuild/
captures/
