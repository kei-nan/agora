# Completed next steps log — entry 80

80. **Corrected a stale claim in `CLAUDE.md`/`docs/project/apps/mobile.md`: mobile is not "a
    TypeScript scaffold missing native projects" — `mobile/android/` is real, committed, and
    has been since commit `0f15f52` ("feat(mobile): add Android NFC passport reader native
    module").** That commit predates this entry; the docs simply never caught up to it. This
    entry re-verified the actual state from scratch rather than trusting either the old docs
    or the assumption that nothing had changed, and updated the two docs to match reality.

    ## What was checked, and how

    - `mobile/android/` is git-tracked in this worktree (confirmed via `git log --oneline --
      mobile/android`, one commit: `0f15f52`). It has real Gradle files (`build.gradle`,
      `settings.gradle`, `gradle/wrapper/gradle-wrapper.properties` pinned to Gradle 8.6),
      generated `app/src/main/res` resources (mipmap icons, styles, manifest), a checked-in
      `app/debug.keystore`, and a hand-written native module: `app/src/main/java/com/agora/
      nfc/NfcPassportModule.kt` + `NfcPassportPackage.kt`.
    - The untracked files at the working-tree root (not visible inside this worktree, read
      directly from the original checkout path instead) explain *how* `android/` was
      generated: `mobile/setup-android.ps1`, a Windows PowerShell script that shells into WSL,
      `npm pack`s `react-native@0.74.0` directly, extracts `template/android` from the tarball,
      and renames `HelloWorld`/`com.helloworld` → `Agora`/`com.agora` — deliberately bypassing
      `npx react-native init`, whose CocoaPods step fails on WSL2 (the script's own header
      comment says so explicitly). `mobile/.idea/` (also untracked, also original-path-only)
      confirms someone subsequently opened the generated project in Android Studio. Neither
      file is part of this session's actual work — both live outside any git worktree — but
      they explain the provenance of the `android/` directory this entry found already
      committed.
    - `cd mobile && npm install`: 1373 packages installed clean (audited 1374; 15 pre-existing
      vulnerabilities reported, all in transitive deps, none newly introduced by this entry —
      not remediated here, out of scope).
    - `npm test`: **4 test suites, 77 tests, all passing** (`sodParser`, `zkProving`,
      `proofEncoding`, `certificateTree` per the existing suite). This is real, checkable
      signal about the JS/TS layer regardless of native build status, and it passes.
    - JDK/Android SDK/Gradle presence in this WSL2 environment, checked directly rather than
      assumed from the prior log entry's claim: `java -version`/`javac -version` — command not
      found (no JDK at all). `ANDROID_HOME`/`ANDROID_SDK_ROOT` — both unset. `adb`/`gradle`/
      `sdkmanager` — none on `PATH`. No SDK under `~/Android`, `~/Android/Sdk`,
      `/usr/lib/android-sdk`, `/opt/android-sdk`, or `/usr/local/android-sdk`. Running
      `./gradlew --version` inside `mobile/android` confirms the concrete failure point:
      `ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH` — Gradle
      itself never gets far enough to report its own version. **The prior log entry's "no JDK,
      Android SDK, or device in this environment" is still accurate for this WSL2 side**, even
      though `mobile/android/` itself is no longer missing — the setup script and `.idea/`
      folder are strong evidence the actual Android Studio work has been happening on the
      Windows host, not in WSL2, which is consistent with nothing showing up here.
    - Per instructions, no multi-gigabyte unattended Android SDK/JDK install was attempted —
      that's a large, slow, license-gated action better confirmed with the user first. What's
      needed instead, so this is a fast follow-up rather than a fresh investigation next time:
      a JDK 17 (React Native 0.74's documented requirement, and what Gradle 8.6 + AGP expect
      for Android builds — `mobile/android/build.gradle` pins `compileSdkVersion 34`,
      `buildToolsVersion 34.0.0`, `ndkVersion 26.1.10909125`), the Android SDK command-line
      tools with `platform-tools`, `platforms;android-34`, `build-tools;34.0.0`, and (for the
      native module specifically) NDK `26.1.10909125` via `sdkmanager`. With those present,
      `cd mobile/android && ./gradlew assembleDebug` is the next concrete command to run.
    - `desktop/cuda-keyring_1.1-1_all.deb` (untracked, repo root) — noted, not touched; unrelated
      to mobile and outside this entry's scope.

    ## What changed, in the docs

    - `CLAUDE.md`: "Mobile: TypeScript scaffold exists but is not a runnable React Native
      project yet" → describes `android/` as present with a real native NFC module, JS test
      suite passing, and JDK/Android SDK as the actual remaining gap in this WSL2 environment
      (not "native projects missing"). The `Mobile: ...` monorepo-structure-diagram line and
      remaining-work item 1 updated the same way — item 1 no longer says the mobile app itself
      gates the first ZK verification test on `react-native init`; it now names the real
      current blocker (JDK/SDK absent in WSL2; `ios/` still doesn't exist at all).
    - `docs/project/apps/mobile.md`: replaced the "missing only the native Android/iOS
      projects (generated by `react-native init`)" framing and its `npx react-native init`
      instructions (which describe a step that already happened, via a different mechanism —
      `setup-android.ps1`'s `npm pack` extraction, not `react-native init`) with the actual
      current state: `android/` real and committed, `ios/` confirmed absent (checked directly:
      no `mobile/ios/` at either the worktree or the original repo path), JS test suite status,
      and the JDK/Android SDK gap with the specific versions the existing Gradle config
      expects.

    ## What remains, honestly

    - **No JDK, Android SDK, or `adb`/device in this WSL2 environment — unchanged from the
      prior log entry**, confirmed fresh rather than assumed. `mobile/android/`'s existence
      does not change this; it only means the SDK/JDK gap is now the *only* thing blocking a
      real `./gradlew assembleDebug`, not "no native project exists at all."
    - **No actual Gradle build was attempted beyond `./gradlew --version`** (which fails before
      reaching Gradle's own version check, on the missing-`JAVA_HOME` error above) — per
      instructions, installing a JDK/SDK to go further was deliberately left as a follow-up
      requiring user confirmation, not done unattended in this session.
    - **`mobile/ios/` still does not exist** — confirmed directly, not assumed from stale docs.
      Nothing in this entry starts that work.
    - **The Android setup script and `.idea/` metadata are themselves untracked** and specific
      to the original working tree / Windows host, not this worktree or this session's commit
      — they're described here for provenance, not brought into version control by this entry.
