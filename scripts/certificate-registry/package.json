{
  "name": "certificate-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain tool that builds our own Document Signer Certificate Merkle tree (see buildTree.ts doc comment) — never bundled into the mobile app or runtime.",
  "type": "module",
  "scripts": {
    "build-tree": "tsx buildTree.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@iden3/js-merkletree": "^1.5.2",
    "@noble/hashes": "^2.2.0",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "5.0.4",
    "vitest": "^2.1.0"
  }
}
